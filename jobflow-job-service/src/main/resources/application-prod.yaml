spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/jobflow

  data:
    redis:
      host: redis
      port: 6379

  kafka:
    bootstrap-servers: kafka:29092

  elasticsearch:
    uris: http://elasticsearch:9200
